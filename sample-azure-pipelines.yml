trigger: none
pool:
  name: production
  demands: agent.os -equals Linux
variables:
#- group: PROD
- name: env
  ${{ if eq(variables['Build.SourceBranchName'], 'dev') }}:
    value: a
  ${{ else }}:
    value: b

steps:
- script: | 
    echo '$(name)'
    echo '$(env)'